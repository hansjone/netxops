# DSH host packages (@deepseek-ai/*) are provided by the Harness profile, not npm.
# Bare clone install must not auto-fetch peers (e.g. dsh-api-remotes → dsh-type-meta 404).
auto-install-peers=false
strict-peer-dependencies=false
